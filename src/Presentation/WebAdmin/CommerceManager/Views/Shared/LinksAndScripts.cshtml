@using System.Web.Optimization

@*
    The file ~/Content/favicon.ico is added via a link. The file doesn't show up in VS. Need to open csproj to see it.
*@

@* 
Can't minify jquery UI CSS because it has 14 imports 

@Html.Partial("Fonts")

@* Load core CSS *@

@* Load partner CSS *@

@* Load third party CSS *@
    @Styles.Render("~/content/bootstrap")
    @Styles.Render("~/content/bootstrap-theme")
    @Styles.Render("~/content/toastr")
    @Styles.Render("~/content/css")
 